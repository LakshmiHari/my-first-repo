pip install -r requirements.txt
uvicorn app:app --reload

Missing-value handling, Outlier detection using IQR, Skewness analysis for fare, Survival breakdown by sex, pclass, and both combined.
Correlation matrix
